// Height of the HeaderBar content row (excludes safe-area top inset).
// Anchored overlays that need to sit just below the HeaderBar
// (e.g., ChatHeaderMenu) read this value to compute their offset
// without duplicating the literal.
export const HEADER_BAR_HEIGHT = 48
