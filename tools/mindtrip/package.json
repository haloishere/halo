{
  "name": "@halo/mindtrip-tool",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Standalone Mindtrip AI travel chat client — cookie session, pure fetch, no SDK. Isolated from @halo/api until ready to bind.",
  "bin": {
    "mindtrip": "./bin/mindtrip.mjs"
  },
  "main": "./src/index.ts",
  "scripts": {
    "chat": "tsx src/cli.ts chat",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
